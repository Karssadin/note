---
up: 
  - "[[数学]]"
down: 
relation: 
  - "[[字符串模拟高精度]]"
题目:
---
- 大整数相加 位数10^6
- 大整数相减 位数10^6
- 大整数*小整数 位数10^6 大小10^9
- 大整数/小整数

---

- 大整数相加
- 大整数存储：存到数组里面：第0位存个位比较好，整数相加会进位，在高位补数也就是数组的末尾补数比较容易
- 运算：Ai+Bi+进位t(0,1)；最后Ai、Bi不存在的话需要看进位有没有

```C
\#include <iostream>
\#include <vector>
using namespace std;

vector<int> add(vector<int> &A,vector<int>&B){
    vector <int> c;
    int t=0;
    for(int i=0;i<A.size()||i<B.size();i++){
        if(i<A.size())
            t+=A[i];
        if(i<B.size())
            t+=B[i];

        //取余得结果
        c.push_back(t%10);

        //进位
        t/=10;
    }

    //查看是否有进位
    if(t)
        c.push_back(1);
    return c;
}

int main(){
    ios::sync_with_stdio(false);

    string a,b;
    vector<int> A,B;

    cin>>a>>b;

    for(int i=a.size()-1;i>=0;i--)
        A.push_back(a[i]-'0');
    for(int i=b.size()-1;i>=0;i--)
        B.push_back(b[i]-'0');

    auto C= add(A,B);

    for(int i=C.size()-1;i>=0;i--)
        printf("%d",C[i]);

    return 0;
}
```

---

- 大整数相减
- 大整数存储：存到数组里面：第0位存个位比较好，整数相加会进位，在高位补数也就是数组的末尾补数比较容易
- 运算：Ai-Bi-t>=0直接减———Ai-Bi-t，Ai-Bi-t<0加10之后减Ai-Bi-t+10，并把借位送给t。默认A>B,如果A<B 计算-(B-A)
- Ai+Bi+进位t(0,1)；Ai或Bi不存在的话就看进位有没有

```C
\#include <iostream>
\#include <vector>

using namespace std;
bool cmp(vector<int>&A,vector<int>&B){
    if(A.size()!=B.size())
        return A.size()>B.size();
    for(int i=A.size()-1;i>=0;i--)
        if(A[i]!=B[i])
            return A[i]>B[i];

    return true;
}

vector<int > sub(vector<int>&A,vector<int>&B){
    vector<int> c;

    int t=0;
    for(int i=0,t=0;i<A.size()||i<B.size();i++){
        t=A[i]-t;
        if(i<B.size())
            t-=B[i];

        //+10取余得当前位数
        c.push_back((t+10)%10);
        //如果t<0说明进行了借位，下一个-1
        if(t<0)
            t=1;
        else
            t=0;
    }
    while(c.size()>1&&c.back()==0)
        c.pop_back();

    return c;
}

int main()
{
    ios::sync_with_stdio(false);

    string a,b;
    cin>>a>>b;

    vector<int> A,B,C;
    for(int i=a.size()-1;i>=0;i--)
        A.push_back(a[i]-'0');

    for(int i=b.size()-1;i>=0;i--)
        B.push_back(b[i]-'0');

    if(cmp(A,B)){
        C=sub(A,B);
        for(int i=C.size()-1;i>=0;i--)
            printf("%d",C[i]);
    }
    else{
        C=sub(B,A);
        printf("-");
        for(int i=C.size()-1;i>=0;i--)
            printf("%d",C[i]);
    }
    return 0;
}
```

---

- 大整数*小整数
- 大整数存储：存到数组里面：第0位存个位比较好，整数相加会进位，在高位补数也就是数组的末尾补数比较容易
- 运算：Ai_b 直接×整个B而不是每个位数相乘，C push (A[i]_b+t)%10 t=(B*A[i]+t)/10
- 为什么整除10是进位？ 因为高位的是低位的10倍。这是十进制 C2:10^2
- 最后判断是否有进位t，如果有的话，直接push到C里面
- 别忘了pop尾数的0

```C
\#include <iostream>
\#include <vector>
using namespace std;

vector <int> mul(vector<int>&A,int B){
    vector<int> c;

    int t=0;
    for(int i=0;i<A.size()||t;i++){
        if(i<A.size())
            t+=B*A[i];
        c.push_back(t%10);
        t/=10;
    }
    while (t) {
        C.push_back(t % 10);
        t /= 10;
    }
    while(c.size()>1&&c.back()==0)
        c.pop_back();
    return c;
}

int main(){
    ios::sync_with_stdio(false);
    string a;
    int b;
    cin>>a>>b;
    vector<int> A;
    for(int i=a.size()-1;i>=0;--i)
        A.push_back(a[i]-'0');

    vector<int> c=mul(A,b);
    for(int i=c.size()-1;i>=0;--i)
        cout<<c[i];

    return 0;
}
```

---

- 大整数/小整数
- 存储来说，不需要0位存个位了，按顺序来说比较容易
- 依旧需要去除高位的0

```C
\#include <iostream>
\#include <vector>
\#include <algorithm>
using namespace std;
int div(vector<int> &A,int B,vector <int>&C){
    int t=0;
    for(int i=A.size()-1;i>=0;--i){

        t=t*10+A[i];
        C.push_back(t/B);
        t%=B;
    }
        reverse(C.begin(),C.end());
    while(C.size()>1&&C.back()==0)
        C.pop_back();
    return t;
}
int main(){
    ios::sync_with_stdio(false);
    vector<int >A,C;
    string a;
    int b;
    cin>>a>>b;
    for(int i=a.size()-1;i>=0;--i)
        A.push_back(a[i]-'0');

    int ret=div(A,b,C);
    for(int i=C.size()-1;i>=0;--i)
        cout<<C[i];

    cout<<endl<<ret;
    return 0;
}
```