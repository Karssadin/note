---
tags:
  - 算法
up:
  - "[[排序]]"
  - "[[数据结构与算法]]"
down:
relation:
---

将数组分为已排序和未排序两部分。每次从未排序部分取出一个元素，在已排序部分中找到正确位置并插入。

- 时间复杂度：平均/最坏 O(n²)，最好 O(n)（已有序）
- 空间复杂度：O(1)
- **稳定排序**

```cpp
void insertionSort(vector<int>& arr) {
    for (int i = 1; i < arr.size(); i++) {
        int key = arr[i];
        int j = i - 1;
        while (j >= 0 && arr[j] > key) {
            arr[j + 1] = arr[j];
            j--;
        }
        arr[j + 1] = key;
    }
}
```

插入排序在**小规模数据**和**基本有序数据**上表现优异。STL 的 `std::sort` 在子数组规模小于阈值（通常 16）时会切换为插入排序。
