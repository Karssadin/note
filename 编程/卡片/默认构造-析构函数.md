---
tags:
  - C++
up:
  - "[[面向对象]]"
down:
relation:
  - "[[构造函数和析构函数]]"
---
- 默认构造函数：如果你为类声明了其他构造函数，编译器不再为你提供一个隐式的默认构造函数。但你可以使用 = default 来显式地要求编译器为你提供一个默认构造函数。
- 拷贝构造,赋值运算符`ClassType & operator =`、移动构造函数、移动赋值运算符、析构函数
- =delete默认是删除这个函数，在C11之前想要删除函数，通常采用的是rivate
