---
tags:
  - 编程
  - 八股文
up: 
down:
  - "[[冒泡排序]]"
relation:
  - "[[C++]]"
  - "[[STL]]"
  - "[[刷题/归档/数据结构|数据结构]]"
---

# 字符串处理

```C++
\#include <string>
using namespace std;

string str = "hello";
int len = str.size();
string sub_str = str.substr(0, 2); //he;
string app_str = str.append(" world"); //hello world
app_str = str.append("22", 2); //hello2222
string rep_str = str.replace(1, 1, "b");//hbllo
int com_res = str.compare(rep_str);
str.push_back("1");
str.pop_back();

```

- 检查是否是数字:bool isdigit(char c)
- 检查是否是字母:bool isalpha(char c)
- 检查是否是大写字母:bool isupper(char c)
- 检查是否是小写字母:bool islower(char c)
- 检查是否是字母或数字:bool isalnum(char c)
- 检查是否是空白字符（如空格、制表符、换行符等）:bool isspace(char c)
- 转换为大写:char toupper(char c)
- 转换为小写:char tolower(char c)

```C++
\#include <sstream>

//istringstream其底层操作与标准输入流的处理方式一致。
// 对 istringstream 对象使用流提取运算符 >> 时，它默认会使用空白字符（如空格、制表符、换行符）作为分隔符。
//使用逗号分割

//getline() 用于从输入流中读取一行文本，并存储到给定的 string 中。
// 可以指定一个可选的分隔符字符，来代替默认的换行符

string line;
getline(cin, line);

istringstream iss(line);
vector<string> tokens;
string token;
while (getline(iss, token, ',')) {  // 使用逗号作为分隔符
    tokens.push_back(token);
}
//使用空格分割
string line;
getline(cin, line);  // 读取一行

istringstream iss(line);  // 初始化istringstream
vector<int> nums;
int num;
while (iss >> num) {  // 从iss中读取整数
    nums.push_back(num);
}
```

# 排序

[![](https://cdn.nlark.com/yuque/0/2023/png/25992891/1698931524259-add88cfc-6644-4a19-92e6-dda758a2a316.png)](https://cdn.nlark.com/yuque/0/2023/png/25992891/1698931524259-add88cfc-6644-4a19-92e6-dda758a2a316.png)


### ![[冒泡排序]]

### ![[插入排序]]

### ![[归并排序]]

### ![[快速排序]]


# 树

### 遍历

### 深度优先搜索：前序、中序、后序的递归、迭代实现

- 迭代法:直接root入栈

```C
vector<int> inorderTraversal(TreeNode* root) {
    stack<TreeNode*> st;
    vector<int> res;
    if(root)
        st.push(root);
    while(!st.empty()){
        TreeNode* tmp = st.top();
        if(tmp){
            st.pop();

            /*
            后续
            st.push(tmp);
            st.push(nullptr);
             */
            if(tmp->right)
                st.push(tmp->right);
            /*
            中序
            st.push(tmp);
            st.push(nullptr);
             */
            if(tmp->left)
                st.push(tmp->left);

            /*
            前序
            st.push(tmp);
            st.push(nullptr);
             */
        }else{
            st.pop();
            tmp = st.top();
            st.pop();
            res.push_back(tmp->val);
        }
    }
    return res;
}
```

- 递归

```C
void Traversal(TreeNode* cur, vector<int>& res){
    if(cur == nullptr)
        return ;
    res.push_back(cur->val);
    Traversal(cur->left, res);
    Traversal(cur->right, res);
}
```

---

### 广度优先搜索：层次遍历

- 迭代

```C
vector<vector<int>> levelOrder(TreeNode* root) {
    queue<TreeNode*> que;
    vector<vector<int>> res;
    if(root)
        que.push(root);
    while(!que.empty()){
        int size = que.size();
        vector<int> vec;
        for(int i = 0; i < size; i++){
            TreeNode* tmp = que.front();
            que.pop();
            vec.push_back(tmp->val);
            if(tmp->left)
                que.push(tmp->left);
            if(tmp->right)
                que.push(tmp->right);
        }
        res.push_back(vec);
    }
    return res;
}
```

- 递归

```C
void order(TreeNode* cur, vector<vector<int>>& res, int depth){
    if(!cur)
        return ;
    if(res.size() == depth)
        res.push_back(vector<int>());
    res[depth].push_back(cur->val);
    order(cur->left, res, depth + 1);
    order(cur->right, res, depth + 1);
}
vector<vector<int>> levelOrder(TreeNode* root) {
    vector<vector<int>> res;
    order(root, res, 0);
    return res;
}
```

# 深度优先搜索

```C++
vector<vector<int>> adjList;
vector<bool> visited(adjList.size(), false);
void DFS(int curr, vector<bool> &visited) {
    visited[curr] = true;
    cout << curr << " ";
    for (int neighbor : adjList[curr]) {
        if (!visited[neighbor]) {
            DFS(neighbor, visited);
        }
    }
}
```

# 广度优先搜索

```C++
vector<vector<int>> adjList;

void BFS(int start) {
    queue<int> q;
    vector<bool> visited(adjList.size(), false);
    vector<int> distance(adjList.size(), INT_MAX);
    visited[start] = true;
    distance[start] = 0;
    q.push(start);

    while (!q.empty()) {
        int curr = q.front();
        q.pop();
        cout << curr << " ";
        //这里可以添加一个for循环，遍历当前队列中的所有结点，这样可以分层遍历
        for (int neighbor : adjList[curr]) {
            if (!visited[neighbor]) {
                visited[neighbor] = true;
                distance[neighbor] = distance[curr] + 1;
                //可以不是1，可以是当前结点的权重
                q.push(neighbor);
            }
        }
    }
}
```