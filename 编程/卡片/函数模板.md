---
tags:
up:
  - "[[C++]]"
down:
  - "[[模板注意事项]]"
  - "[[普通函数与函数模板的区别]]"
  - "[[模板局限性]]"
  - "[[类模板和函数模板的区别]]"
  - "[[模板实例化与分支裁剪]]"
relation:
  - "[[类模板]]"
---
- 作用：建立一个通用函数，其返回值类型和形参类型可以不具体指定，用一个虚拟的类型来代表
    - 将类型参数化
- 如果有函数的实现逻辑相似的话，可以使用模板来进行实现，比如交换两个数字，不用数据类型要写好多函数，使用模板即可

```C
/*
template<typename T>
函数声明或定义

template ---声明创建模板
typename ---表面其后面的符号是一种数据类型，可以用class代替
T ---通用的数据类型，名称可以替换，通常为大写字母
*/

//交换两个数字模板
template<typename T>
void mySwap(T&a,T&b){
	T tmp=a;
	a=b;
	b=tmp;
}

//使用方法
//1、自动推导类型
mySwap(a,b);
//2、显示指定为特定数据类型
mySwap<int>(a,b);
```

1. [[模板注意事项]]
2. [[普通函数与函数模板的区别]]
3. [[模板局限性]]
4. [[类模板和函数模板的区别]]