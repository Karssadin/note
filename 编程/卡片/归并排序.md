---
tags:
  - 算法
up:
  - "[[排序算法总结]]"
down:
relation:
  - "[[递归与分治]]"
  - "[[快速排序]]"
---

# 归并排序

归并排序把序列递归拆成两半，分别排序后再合并两个有序区间。

```cpp
void mergeSort(vector<int>& a, int l, int r, vector<int>& tmp) {
    if (l >= r) return;
    int mid = l + (r - l) / 2;
    mergeSort(a, l, mid, tmp);
    mergeSort(a, mid + 1, r, tmp);
    int i = l, j = mid + 1, k = l;
    while (i <= mid && j <= r) tmp[k++] = a[i] <= a[j] ? a[i++] : a[j++];
    while (i <= mid) tmp[k++] = a[i++];
    while (j <= r) tmp[k++] = a[j++];
    for (int p = l; p <= r; ++p) a[p] = tmp[p];
}
```

## 特点

1. 时间复杂度稳定为 O(n log n)。
2. 空间复杂度通常为 O(n)。
3. 稳定排序。
4. 适合链表排序和外部排序。
5. 思想是典型的分治。
