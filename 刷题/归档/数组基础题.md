---
up:
  - "[[刷题/归档/数组]]"
down:
  - "[[离散化]]"
---
---

# 遍历

- [485. 最大连续 1 的个数](https://leetcode.cn/problems/max-consecutive-ones/)
- 统计sum、比较Max、更新Max，返回Max

---

- [495. 提莫攻击](https://leetcode.cn/problems/teemo-attacking/)
- `res += min(duration, timeSeries[i] - timeSeries[i - 1]);`，每次对res判断，这次与上次之间的间隔，最后res 再加一个duration

---

- [414. 第三大的数](https://leetcode.cn/problems/third-maximum-number/)
- 一次遍历即可，判断x是否大于最大，判断最大是否大于x & x是否大于第二大，判断x是否大于第三大 & 第二大是否大于x。

---

- [628. 三个数的最大乘积](https://leetcode.cn/problems/maximum-product-of-three-numbers/)
- 遍历找到最大的三个数，和最小的两个数。返回max（,）

---

- [645. 错误的集合](https://leetcode.cn/problems/set-mismatch/)
- 遍历，开辟额外空间找到多的元素，使用异或找到缺失元素和重复元素的异或值，返回即可

---

- [697. 数组的度](https://leetcode.cn/problems/degree-of-an-array/)
- 遍历，利用map(int, vector<int>)存储数字跟数字出现次数、首位、末尾，判断更新max_len与max_sum

---

- [453. 最小操作次数使数组元素相等](https://leetcode.cn/problems/minimum-moves-to-equal-array-elements/)
- 对n - 1个数字 + 1，相当于对1 个数字 - 1，遍历一次判断当前数字多少次可以到最小。

---

- [665. 非递减数列](https://leetcode.cn/problems/non-decreasing-array/)
- 遍历，针对出现nums[i + 1] < nums[i]的情况，有两种操作，分别操作判断是否非递减

# 原地哈希

- [448. 找到所有数组中消失的数字](https://leetcode.cn/problems/find-all-numbers-disappeared-in-an-array/)
- 遍历，如果数字出现将对应索引位置置为负数，之后再遍历判断是否为正数
- 遍历，将对应索引位置+ n，之后遍历判断是否小于n
- 这两个方法中都需要注意，负数不能直接取索引，需要`abs`，`+n`之后不能直接取索引，需要`%n`

---

- [442. 数组中重复的数据](https://leetcode.cn/problems/find-all-duplicates-in-an-array/)
- 遍历，如果当前数字大于0说明之前没有出现过，置为相反数，如果负数则把数字放到res中
- 遍历，进行交换，多次交换之后会实现，缺失元素的位置是重复元素，如果只出现一次，就是nums[i] == i + 1

```C++
vector<int> result;
for (int i = 0; i < nums.size(); ++i)
    while (nums[i] != nums[nums[i] - 1])
        swap(nums[i], nums[nums[i] - 1]);
for (int i = 0; i < nums.size(); ++i)
    if (nums[i] != i + 1)
        result.push_back(nums[i]);
```

---

- [645. 错误的集合](https://leetcode.cn/problems/set-mismatch/)
- 遍历，如果数字出现判断是否为正数，如果不是，就置为负数，如果是，就说明重复。之后再一次遍历，判断是否大于0，大于说明没有出现过

# 二维数组

- [118. 杨辉三角](https://leetcode.cn/problems/pascals-triangle/)
- 先生成数组，之后resize，填写即可
- [119. 杨辉三角 II](https://leetcode.cn/problems/pascals-triangle-ii/)
- 可以使用滚动数组来减少空间使用，因为只参考之前一行的数据，所以定义cur、pre两个数组就行，不用定义二维数组
- [661. 图片平滑器](https://leetcode.cn/problems/image-smoother/)
- 四层循环即可暴力解

# 三分枚举

- 162.寻找峰值
- 852.山脉数组的峰顶索引
- 剑指 offer II 069.山峰数组的顶部
- 2032.至少在两个数组中出现的值
- 1095.山脉数组中查找目标值
- 1521.找到最接近目标值的函数值
- 1755.最接近目标值的子序列和
- 2035.将数组分成两个数组并最小化数组和的差
- 面试题 16.06.最小差

# 离散化

[[离散化]]
