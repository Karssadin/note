---
up:
  - "[[刷题/归档/数组|数组]]"
  - "[[刷题]]"
---
[LeetCode常见题目](https://leetcode.cn/circle/discuss/48kq9d/)

面试常考

前缀和

二分法中的mid防止溢出

亦或的知识

C++输入

输出

边界问题

分治

取模 mod %

设置边界值

头文件

优先队列priority_queue

预先分配vector空间

使用lambda函数代替cmp，来自定义规则

移除带退格的字符串

C++中二分查找大于等于某个数的第一个位置的功能

### [LeetCode常见题目](https://leetcode.cn/circle/discuss/48kq9d/)

### 面试常考

- **字符串处理；排序算法：快排、归并；双指针；宽度优先搜索；深度优先搜索/递归；动态规划**
- 二分；分治法；二叉树的遍历；拓扑排序

---

- **链表、二叉树、堆、哈希表**
- 字典树、并查集、
- 线段树、树状数组、跳跃表、红黑树

---

常见算法时间复杂度：

- O(n)：双指针、二叉树遍历、n次哈希表、单调栈、单调队列
- O(log n)：二分法、快速幂
- O(nlong n)：n次log n操作

### 前缀和

- 线段树、树状数组
- 定义prefix Sum[i]表示前i个数的和
- i to j 的和可以直接用前缀和解

```C++
vector<int> prefixSum(n, 0);
prefixSum[0] = 0;
for(int i = 0; i < nums.size(); i++){
    prefixSum[i + 1] = prefixSum[i] + nums[i];
}
```

### 二分法中的mid防止溢出

当left=int的最大值/2，right=int的最大值/2+1使用int mid=(left+right)/2会导致int溢出所以使用int mid=left+(right-left)/2

### 亦或的知识

交换律：a ^ b ^ c <=> a ^ c ^ b任何数于0异或为任何数 0 ^ n => n相同的数异或为0: n ^ n => 0

### C++输入

数据多的时候尽量使用scanf或者使用cin时候添加下面的标识

```C
inline void speedup()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}
```

scanf读字符，可能会读空格和回车

如果读字符串的话，会过滤字符串和回车，使用s%来读

### 输出

输出几位小数

```C
printf("%.6f",r);
```

### 边界问题

题目给出最大数量为N

自己定义最大值时N+10最好

### 分治

碰到有大量重复计算的时候，选择使用分治法

快排、归并排序、二分查找[剑指 Offer 16. 数值的整数次方](https://leetcode.cn/problems/shu-zhi-de-zheng-shu-ci-fang-lcof): 实现 pow(x, n) ，即计算 x 的 n 次幂函数（即，xn）。不得使用库函数，同时不需要考虑大数问题。

### 取模 mod %

自然数的取模运算的定义是这样的：

如果a和d是两个自然数，d非零，可以证明存在两个唯一的整数 q 和 r，满足 a = qd + r 且0 ≤ r < d。其中，q 被称为商，r 被称为余数

那么对于负数，是否可以沿用这样的定义呢？

假如我们按照正数求余的规则求 (-10) mod 3 的结果，就可以表示 -10 为 (-4)* 3 +2

其中，2是余数，-4是商

进一步，如果允许余数是负数，它的绝对值小于除数呢，那就

可以表示 -10 为 (-3)* 3 -1。-1 是余数，-3是商。

`C++： cout << (-10) % 3; // 输出 -1`

想让C++的mod为正数，就需要得到mod结果之后+d再对d取模

### 设置边界值

1个字节8位，memset按照自己赋值，0x3f3f3f3f 大于10^9

```C
    \#include <cstring>
    int arr[10];
    memset(arr, 0x3f, sizeof(arr));
```

### 头文件

bits/stdc++.h 不是标准C++头文件，而是一些编译器的非标准头文件。它包含了所有标准库头文件和一些非标准库头文件，是方便编写竞赛程序的一种方法。

这个头文件包含了常见的标准库头文件，如`<iostream>`, `<algorithm>`, `<vector>`, `<map>`等等，以及一些常用的非标准库头文件

### 优先队列priority_queue

默认为大根堆`priority_queue<int> heap;`

改写为小根堆`priority_queue<int,vector<int>,greater> heap;`

### 预先分配vector空间

`vec.reserve(matrix.size() * matrix[0].size());`

### 使用lambda函数代替cmp，来自定义规则

```C
bool cmp(string& x, string& y){
     return x + y < y + x;
}

sort(strs.begin(), strs.end());

//使用lambda函数代替cmp

sort(strs.begin(), strs.end(), [](string& x, string& y){ return x + y < y + x; });
```

### 移除带退格的字符串

指针指向末尾，因为只会删除前面的，所以从后往前遍历即可，做一个计数器来判断是否需要删去

### C++中二分查找大于等于某个数的第一个位置的功能

- lower_bound(x)返回>=x的最小数的迭代器
- upper_bount(x)返回>x的最下数的迭代器
