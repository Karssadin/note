---
tags:
  - 操作系统
up:
  - "[[操作系统基础]]"
down:
relation:
  - "[[死锁产生的必要条件]]"
  - "[[死锁的概念及产生原因]]"
---
四种策略：**预防死锁、避免死锁、检测死锁、解除死锁**

---

- 预防死锁
    - 破坏请求条件：一次性分配所有资源，这样就不会再有请求了；
    - 破坏请求和保持条件：只要有一个资源得不到分配，也不给这个进程分配其他的资源：
    - 破坏不可剥夺条件：当某进程获得了部分资源，但得不到其它资源，则释放已占有的资源；
    - 破坏环路等待条件：系统给每类资源赋予一个编号，每一个进程按编号递增的顺序请求资源，释放则相反

---

- 避免死锁
    - **安全状态**：如果没有死锁发生，并且即使所有进程突然请求对资源的最大需求，也仍然存在某种调度次序能够使得每一个进程运行完毕，则称该状态是安全的。
        - 安全状态的检测与死锁的检测类似，因为安全状态必须要求不能发生死锁。下面的银行家算法与死锁检测算法非常类似，可以结合着做参考对比
    - **银行家算法的核心思想**：当一个进程请求资源时，先检查分配资源后系统是否处于安全状态，如果是，则分配资源；否则，进程必须等待直到系统进入安全状态。
    - 这个“安全状态”的检测算法，主要基于以下步骤：
        - **初始化**：开始时，只知道系统的总资源数量和当前的空闲资源数量。
        - **请求资源**：当一个进程请求资源时，系统先检查是否有足够的资源可供分配。如果没有，进程将被阻塞。如果有，系统会假设它已将资源分配给该进程，并更新资源状态。
        - **检查是否安全**：使用上述描述的算法来检查新的资源状态是否安全。这涉及到检查是否存在一个资源分配序列可以使每个进程都完成。
        - **确定是否分配资源**：
            - 如果新的状态是安全的，系统会正式分配资源。
            - 如果新的状态是不安全的，系统会撤销刚刚做出的假设分配，并使进程等待。

---

- 检测死锁
    - 允许死锁发生，系统定期运行**死锁检测算法**来识别死锁
    - **资源分配图（RAG）检测算法**：
        1. 构建资源分配图：进程节点 + 资源节点，分配边（资源→进程）+ 请求边（进程→资源）
        2. 简化资源分配图：找到一个没有等待资源的进程，释放其所有资源，删除相关边
        3. 重复步骤2，若所有进程都能被删除（图可化简），则无死锁；若存在不可删除的进程集合，则这些进程处于死锁
    - **矩阵检测算法**（适合多资源类型）：
        - 设 `Available[j]` = 资源 j 的可用量
        - 设 `Allocation[i][j]` = 进程 i 持有资源 j 的数量
        - 设 `Request[i][j]` = 进程 i 对资源 j 的当前请求量
        - 算法：找到一个 `Request[i] <= Available` 的进程 i，模拟其运行完毕，释放资源（`Available += Allocation[i]`）；重复直到无法找到这样的进程，剩余进程即死锁进程
    - **检测时机**：定期检测或 CPU 利用率下降时检测（检测本身有开销）

---

- 解除死锁

    - **资源剥夺**：挂起某些死锁进程，并抢占它的资源，将这些资源分配给其他死锁进程（但应该防止被挂起的进程长时间得不到资源）
    - **撤销进程**：强制撤销部分、甚至全部死锁进程并剥夺这些进程的资源（撤销的原则可以按进程优先级和撤销进程代价的高低进行）
    - **进程回退**：让一个或多个进程回退到足以避免死锁的地步。进程回退时自愿释放资源而不是被剥夺。要求系统保持进程的历史信息，设置还原点
