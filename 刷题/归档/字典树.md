---
up:
  - "[[树]]"
---
- 208.实现 Trie (前缀树)
- 剑指 offer II 062.实现前缀树
- 14.最长公共前缀

1. 添加与搜索单词 - 数据结构设计

- 820.单词的压缩编码
- 剑指 Offer II 066.单词之和
- 剑指 Offer Il 064.神奇的字典
- 5302.加密解密宁符串

### 代码实现

- 字典树（Trie）是一种常用的数据结构，它可以用来快速地查询和插入字符串。它的基本思想是，将每个字符串都表示为一个树形结构，根节点表示空字符串，每个节点表示一个字符，每个节点的子节点表示以该节点表示的字符为前缀的所有字符串。
- 高效地存储和查找字符串集合的数据结构

---

```C
\#include <iostream>
using namespace std;

const int N = 100010;
int son[N][26];//因为是只有小写字母，所以每个节点只能向下有26个儿子节点,存放当前节点儿子节点的下标
int cnt[N];//以当前下标结尾的有多少个字符串
int idx;
/*下标为0的，既是根节点，也是空节点
idx相当于一个分配器，如果需要新的节点加入，就++idx分配一个下标，
*/

/*Trie树中有个二维数组 son[N][26]，表示当前结点的儿子，如果没有的话，可以等于++idx。
Trie树本质上是一颗多叉树，对于字母而言最多有26个子结点。所以这个数组包含了两条信息。
比如：son[1][0]=2表示1结点的一个值为a的子结点为结点2;如果son[1][0] = 0，则意味着没有值为a子结点。*/

char str[N];

void insert(char *str)  // 插入字符串
{
    int p = 0;
    for (int i = 0; str[i]; i ++ )
    {
        int u = str[i] - 'a';
        //查看这个编号是否有idx对应上坐标
        if (!son[p][u])
            son[p][u] = ++ idx;
        p = son[p][u];
    }
    cnt[p] ++ ;
}

int query(char *str)  // 查询字符串出现次数
{
    int p = 0;
    for (int i = 0; str[i]; i ++ )
    {
        //找到字母对应子节点的编号
        int u = str[i] - 'a';
        //查看这个编号是否有idx对应上坐标
        if (!son[p][u])
            return 0;
        p = son[p][u];
    }
    return cnt[p];
}
int main(){
    cout.tie(0);
    cin.tie(0);
    ios::sync_with_stdio(false);
    int n;
    cin>>n;
    while (n -- ){
        char op[2];
        cin>>op>>str;

        if(op[0]=='I')
            insert(str);
        else
            cout<<query(str)<<endl;
    }
    return 0;
}
```

### 最大异或对

给定的 N个整数 A1，A2……AN中选出两个进行 xor异或）运算，得到的结果最大是多少？

---

[![](https://www.notion.so./imgs/%E6%9C%80%E5%A4%A7%E5%BC%82%E6%88%96%E5%AF%B9.jpg)](https://www.notion.so./imgs/%E6%9C%80%E5%A4%A7%E5%BC%82%E6%88%96%E5%AF%B9.jpg)

```C
\#include<iostream>
\#include<algorithm>
using namespace std;
int const N=100010,M=31*N;

int n;
int a[N];
int son[M][2],idx;
//M代表一个数字串二进制可以到多长

void insert(int x){
    int p=0;  //根节点
    for(int i=30;i>=0;i--){
        int u=x>>i&1;   /////取X的第i位的二进制数是什么  x>>k&1(前面的模板)
        if(!son[p][u]) son[p][u]=++idx; ///如果插入中发现没有该子节点,开出这条路
        p=son[p][u]; //指针指向下一层
    }
}
int search(int x){
    int p=0;int res=0;
    for(int i=30;i>=0;i--){                               ///从最大位开始找
        int u=x>>i&1;
        if(son[p][!u]){
        ////如果当前层有对应的不相同的数
        ///p指针就指到不同数的地址

          p=son[p][!u];
          res=res*2+1;
             ///*2相当左移一位  然后如果找到对应位上不同的数res+1 例如    001
        }                                                       ///       010
        else{                                            ////          --->011
        //刚开始找0的时候是一样的所以+0    到了0和1的时候原来0右移一位,判断当前位是同还是异,同+0,异+1

            p=son[p][u];
            res=res*2+0;
        }
    }
    return res;
}
int main(void){
    cin.tie(0);
    cin>>n;
    idx=0;
    for(int i=0;i<n;i++){
        cin>>a[i];
        insert(a[i]);
    }
    int res=0;
    for(int i=0;i<n;i++){
        res=max(res,search(a[i]));  ///search(a[i])查找的是a[i]值的最大与或值
    }
    cout<<res<<endl;
}
```
