---
tags: 
up:
  - "[[函数对象-仿函数]]"
  - "[[STL常用算法]]"
down: 
relation:
  - "[[容器存储自定义数据类型指定排序规则]]"
  - "[[sort：对序列进行排序]]"
---
- 对于自定义的数据类型，默认没有排序规则，因此需要自己定义仿函数来指定排序规则。

```C++
\#include <iostream>
\#include <set>
\#include <string>

// 自定义类型
class Person {
public:
    Person(std::string name, int age, int height) 
        : Name(name), Age(age), Height(height) {}

    std::string Name;
    int Age;
    int Height;
};

// 定义仿函数来指定排序规则
class MyCompare {
public:
    bool operator()(const Person &p1, const Person &p2) const {
        if (p1.Age == p2.Age)
            return p1.Height > p2.Height; // 如果年龄相同，则按身高从大到小排序
        return p1.Age > p2.Age; // 否则按年龄从大到小排序
    }
};

int main() {
    // 使用仿函数指定排序规则
    std::set<Person, MyCompare> s2;
    s2.insert(Person("Alice", 30, 160));
    s2.insert(Person("Bob", 25, 175));
    s2.insert(Person("Charlie", 30, 155));

    for (const auto& person : s2) {
        std::cout << person.Name << " " << person.Age << " " << person.Height << std::endl;
    }
    // 输出:
    // Alice 30 160
    // Charlie 30 155
    // Bob 25 175
    return 0;
}
```