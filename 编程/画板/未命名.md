## 启动

- 创建BRPC server，监听。配置信息。

## proto

query_id
slice_id
segment_id
eos
type
trunk

## option
retry 3

 
## 发送数据

- 根据ip 端口构建对应的channel，封装各种信息（query_id、是否为空？）。发送成功、失败等处理逻辑。
- channel的生命周期：执行sql，获取brpc channel时创建。

## 接收数据
- 不需要接收线程。
- 回调函数处理，调用MessageProcessor，判断request中chunk类型。将其放到对应query_id slice_id对应的stream中（block、bloom）



- 是否需要压缩、加密

BRPC是的一个通道是单方面建立的。也就是接收端不管发送端的建立，在接收端重启时它也需要重新建立于其他接收端（这个通信中它处于发送端）。sr中丢失的包就不进行重试。  
qtree如果要使用BRPC：  

2. service中接收数据，处理数据，将数据放到对应buffer中？  
3. 构建发送数据逻辑，
4.关于qtree现有重连重发的机制，可以删除。链接的建立使用BRPC之后，不用去在发送失败后管链接的建立，而是处理cancelled  
5. 发送成功？空block需要在发送队列中等待，等待到最后再进行发送（如果发送队列在排队）。