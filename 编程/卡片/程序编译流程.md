---
tags:
  - C++
up:
  - "[[C++基础知识]]"
down:
relation:
  - "[[ifdef、endif、ifndef]]"
---
- 预处理 -E：处理以 # 开头的指令； .i
- 编译 -S：将源码 .cpp 文件翻译成 .s .asm汇编代码；
- 汇编 -c：将汇编代码 .s 翻译成机器指令 .o 文件；
- 链接：汇编程序生成的目标文件，即 .o 文件，并不会立即执行，因为可能会出现：.cpp 文件中的函数引用了另一个 .cpp 文件中定义的符号或者调用了某个库文件中的函数。那链接的目的就是将这些文件对应的目标文件连接成一个整体，从而生成可执行的程序 -fPIC 文件。
    - 地址修正：根据重定位表，找到函数变量进行修改地址
    - 在链接时候如果没有提供对应的目标文件，就会报错，无法解析的引用、引用未定义、符号未定义

---

- readelf -h 文件，可以找到可执行文件的基本信息，比如操作系统，机器类型
- readelf -S 文件，可以找到可执行文件的区块`section`信息，`test`代码区,`data`数据区（全局变量）
- 不允许直接执行，因为有函数一个语句，将函数的跳转地址设置为0
- objdump -s -d 文件，查看文件内容，可以看到主函数代码，其中的call指令，其中的调准地址为0，在后面链接时候修正
- objdump -r 文件，查看重定位表，可以看其中需要被重定位的函数的偏移地址
