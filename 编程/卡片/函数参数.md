---
tags: 
up: 
  - "[[函数]]"
down: 
relation:
  - "[[临时变量不能作为非const引用的函数参数]]"
---
- 参数默认值
    - 传了就用传递的，没传就用默认值
    - 函数名(参数=默认值)
    - **默认参数必须在参数列表末尾**，也就是有默认值的参数后面的参数列表不可以有无默认值的参数
    - 声明有默认参数，实现就不能有默认参数，**二者只能有一个位置有默认参数**
- 参数列表中的占位参数，利用数据类型来占位，但是没有变量名
    - 运算符重载中有用处
    - 占位参数也可以友默认值