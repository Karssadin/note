---
tags: 
up: 
  - "[[面向对象]]"
down: 
relation:
  - "[[虚函数与虚函数表]]"
  - "[[虚函数表原理]]"
---
- 编译时：vtable是在编译时为每个有虚函数的类生成的。编译器知道每个类及其虚函数，因此可以为每个类生成一个静态的vtable。
- 运行时：当创建具有虚函数的对象时，其构造函数会设置vptr，使其指向该类的vtable。如果对象是派生类的实例，并且它重写了基类的虚函数，那么它的vptr将指向其派生类的vtable。这是动态多态的关键。