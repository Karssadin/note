---
tags: 
up:
  - "[[运输层：TCP UDP]]"
down:
  - "[[粘包原因？]]"
  - "[[怎么避免粘包？]]"
relation:
---

- 当进行数据传输时，由于多种原因，可能会导致发送方发送的多个数据包被TCP层合并为一个数据包发送，或者接收方一次性接收了多个数据包的数据
- TCP是面向流的协议，它只保证数据的有序和完整到达，但不保证数据以发送时的消息边界到达。而UDP是面向报文的，每个报文都会保留其边界信息，并以报文为单位进行发送和接收，因此UDP不容易出现粘包问题。
