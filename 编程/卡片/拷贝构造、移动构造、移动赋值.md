---
tags:
  - STL
up:
  - "[[面向对象]]"
down:
relation:
  - "[[深拷贝与浅拷贝]]"
  - "[[拷贝构造函数形参为什么不能用值传递或者指针传递]]"
  - "[[拷贝构造函数的调用规则]]"
  - "[[拷贝构造函数与拷贝赋值和移动赋值的区别？]]"
  - "[[拷贝初始化和直接初始化的区别]]"
  - "[[拷贝构造函数参数中为什么要加const]]"
---
- 类中包含指针变量时需要重载拷贝构造函数、赋值运算符和析构函数
- ==**当你用一个对象初始化另一个对象时，会调用拷贝构造函数或移动构造函数。**==
- ==**当你将一个对象赋给另一个已经存在的对象时，会调用赋值操作符或移动赋值操作符**==

```C++
class MyClass {
public:
//拷贝构造函数
    MyClass(const MyClass& other) {
        // 将 "other" 的数据成员复制到当前对象
    }
//移动构造函数
    MyClass(MyClass&& other) noexcept {
        // 将 "other" 的资源移动到当前对象
        this.data = other.data;
        other.data = nullptr;
        // 然后使 "other" 进入一个有效但未定义的状态
    }
  //赋值构造函数
    MyClass& operator=(const MyClass& other) {
        if (this != &other) {  // 自赋值检查
            // 释放当前对象已经持有的资源（如果有）
            // 将 "other" 的数据成员拷贝到当前对象
        }
        return *this;
    }
//移动赋值函数
    MyClass& operator=(MyClass && other) noexcept {
        if (this != &other) {  // 自赋值检查
            // 释放当前对象已经持有的资源（如果有）

            // 将 "other" 的资源移动到当前对象
            // 然后使 "other" 进入一个有效但未定义的状态
        }
        return *this;
    }
};
// noexcept是从 C++11 开始的新特性，它表示该函数不会抛出异常。为移动构造函数和移动赋值操作符标记 noexcept 是一个好习惯，因为它们通常不应该抛出异常，同时这有助于某些容器（如 std::vector）优化其行为。
```

---

- 拷贝初始化和直接初始化的区别?
    - 拷贝初始化和直接初始化调用的构造函数是不一样的，但是当类进行复制时，如果拷贝的不是一个现有的类，会自动生成一个临时的对象，然后再进行拷贝初始化。
- 拷贝构造函数参数中为什么有时候要加const?
    - 当参数为一个临时对象的时候，临时对象是一个右值。而拷贝构造函数的参数中，如果不加const，那么就是一个非常量左值引用（非常量左值是不能引用右值的），加了const之后就是一个常量左值引用，可以引用右值。
    - 如果一个参数是以**非const引用**传入，c++编译器就有理由认为程序员会在函数中修改这个值，并且这个被修改的引用在函数返回后要发挥作用。
    - 但如果你把一个临时变量当作非const引用参数传进来，由于临时变量的特殊性，程序员并不能操作临时变量，而且临时变量随时可能被释放掉，所以，一般说来，修改一个临时变量是毫无意义的，据此，c++编译器加入了**临时变量不能作为非const引用**的这个语义限制
