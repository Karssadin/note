---
tags:
  - 操作系统
up:
  - "[[IO与进程间通信]]"
down:
relation:
  - "[[命名管道]]"
---
- 它是半双工的，具有固定的读端和写端，数据流是单向的
- 它只能用于**父子进程或者兄弟进程**之间的进程的通信；
- 它可以看成是一种特殊的文件，对于它的读写也可以使用普通的 read、write 等函数。但是它不是普通的文件，并不属于其他任何文件系统，并且只存在于内存中。
