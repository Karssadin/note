---
tags: 
up:
  - "[[进程与线程]]"
down: 
relation:
  - "[[线程的概念和开销]]"
---
预先创建的线程集合。这些线程是闲置的，等待分配任务执行。
    - 快速响应：由于线程已经预先创建，所以当有新的任务到来时，可以立即由空闲线程开始执行，而不必等待新线程的创建。
    - 减少开销：预先创建和重复利用线程，避免了频繁创建和销毁线程的开销。
    - 资源利用有限和可控：限制了线程的最大数量，避免了因大量并发线程而导致的系统资源消耗过多。
    - 提高系统稳定性：通过限制最大线程数，可以预防因过多的线程耗尽系统资源导致的崩溃。