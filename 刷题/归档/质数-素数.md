---
up:
  - "[[数学]]"
---
### 试除法判定

```C
\#include <bits/stdc++.h>
using namespace std;

bool is_prime(int n){
    if(n < 2) return false;
    // for(int i=2;i<n;i++)
    for(int i = 2;i <= n / i;i ++)
        if(n % i == 0)
            return false;

    return true;
}
int main(){
    int n;
 cin >>n;
    while(n--){
        int x;
        cin>>x;
        if(is_prime(x))
            puts("Yes");
        else
            puts("No");
    }
    return 0;
}
```

### 分解质因数

```C
\#include <iostream>
using namespace std;

void divide(int x){
    for(int i=2;i<=x/i;i++)
        if(x%i==0){
            int s=0;
            int c=x;
            while(x%i==0){
                x/=i;
                s++;
            }
            cout<<i<<" "<<s<<endl;
        }
    if(x>1)
        cout<<x<<" 1"<<endl;
}
int main(){
    int n;
    cin>>n;
    while(n--){
        int x;
        cin>>x;
        divide(x);
        cout<<endl;
    }
}
```

```C
\#include<iostream>
\#include<vector>

using namespace std;
const int N = 1000010;
int n;
//primes数组用来存放质数
int primes[N], cnt;
//st[i], i为质数则为false否则为true
bool st[N];

void get_primes2(){
    for(int i=2;i<=n;i++){

        if(!st[i]) primes[cnt++]=i;//把素数存起来
        for(int j=i;j<=n;j+=i){//不管是合数还是质数，都用来筛掉后面它的倍数
            st[j]=true;
        }
    }
}
void get_primes1(){
    for(int i=2;i<=n;i++){
        //只筛掉质数的倍数，合数之前已经筛过了，把循环放到if里面就行
        if(!st[i]){
            primes[cnt++]=i;
            for(int j=i;j<=n;j+=i) st[j]=true;//可以用质数就把所有的合数都筛掉；
        }
    }
}
void get_primes(){
    //外层从2~n迭代，因为这毕竟算的是1~n中质数的个数，而不是某个数是不是质数的判定
    for(int i=2;i<=n;i++){
        if(!st[i]) primes[cnt++]=i;
        for(int j=0;primes[j]<=n/i;j++){//primes[j]<=n/i:变形一下得到——primes[j]*i<=n,把大于n的合数都筛了就
        //没啥意义了
            st[primes[j]*i]=true;//用最小质因子去筛合数


//                prime[]数组中的素数是递增的,当i能整除prime[j]，那么i*prime[j+1]这个合数肯定被prime[j]乘以某个数筛掉。
//    因为i中含有prime[j],prime[j]比prime[j+1]小，即i=k*prime[j]，那么i*prime[j+1]=(k*prime[j])*prime
//    [j+1]=k’*prime[j]，接下去的素数同理。所以不用筛下去了。因此，在满足i%prime[j]==0这个条件之前以及第一次
//    满足改条件时,prime[j]必定是prime[j]*i的最小因子
            if(i%primes[j]==0) break;//每个合数只会被它的最小素因子筛掉，就把复杂度降到了O(N)
        }
    }

}
int main()
{
    cin >> n;
    get_primes2();
    cout << cnt << endl;
    return 0;
}
```
