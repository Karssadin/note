---
tags: 
up: 
  - "[[面向对象]]"
down: 
relation:
  - "[[空类、空对象的大小]]"
---
- 与普通成员变量有关，与成员函数和静态成员无关。即普通成员函数，静态成员函数，静态数据成员，静态常量数据成员均对类的大小无影响。因为静态数据成员被类的对象共享，并不属于哪个具体的对象。
- 虚函数对类的大小有影响，是因为虚函数表指针的影响，基类中如果为虚函数的话，会有该指针(所以，空类中只有该虚函数的话，会有一个指针的空间)，指向基类的vftable虚函数表