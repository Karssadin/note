---
up:
  - "[[贪心]]"
---
- 区间问题排序

### 区间合并

- [763. 划分字母区间](https://leetcode.cn/problems/partition-labels/)
- 类似将区间合并，最少合并次数，每个区间中可以有多个覆盖区间
- 统计一下字母出现的最后位置，之后遍历，l = 0, r = 0，r一直更新为当前遇到的字母的最后边界，如果i碰到r，就说明当前已经位于l - i中所有字符的最远边界之外了，更新l、res

```C
vector<int> partitionLabels(string s) {
    int hash[27] = {0};
    for(int i = 0; i < s.size(); i++)
        hash[s[i] - 'a'] = i;
    vector<int> res;
    int l = 0, r = 0;
    for(int i = 0; i < s.size(); i++){
        r = max(r, hash[s[i] - 'a']);//计算当前出现的最远边界
        if(i == r){
            //当前已经位于l - i中所有字符的最远边界之外了
            res.push_back(r - l + 1);
            l = i + 1;
        }
    }
    return res;
}
```

---

- [56. 合并区间](https://leetcode.cn/problems/merge-intervals)
- 区间问题就是先排序，之后处理
- 按左端点排序，之后判断是否重叠，放进res

```C
vector<vector<int>> merge(vector<vector<int>>& intervals) {
    vector<vector<int>> result;
    if (intervals.size() == 0) return result; // 区间集合为空直接返回
    sort(intervals.begin(), intervals.end(), [](const vector<int>& a, const vector<int>& b){return a[0] < b[0];});
    result.push_back(intervals[0]); // 第一个区间就可以放进结果集里，后面如果重叠，在result上直接合并
    for (int i = 1; i < intervals.size(); i++) {
        if (result.back()[1] >= intervals[i][0])
            // 发现重叠区间,合并区间，只更新右边界就好，因为result.back()的左边界一定是最小值，因为我们按照左边界排序的
            result.back()[1] = max(result.back()[1], intervals[i][1]);
         else
            result.push_back(intervals[i]); // 区间不重叠
    }
    return result;
}
```

### 区间选点

- 给定 N 个闭区间 [ai,bi]，请你在数轴上选择尽量少的点，使得每个区间内至少包含一个选出的点。
- 输出选择的点的最小数量。位于区间端点上的点也算作区间内。

---

- 思路：
- 每个区间按照右端点从小到大排序
- 从前往后依次枚举每个区间，如果当前区间中已经包含点，直接continue，否则选择当前区间的右端点

---

```C
\#include <bits/stdc++.h>
using namespace std;
const int N =100010;
int n;

struct Range{
    int l,r;
    bool operator< (const Range &W)const{
        return r < W.r;
    }
}range[N];
int main(){
    cin>>n;
    for(int i=0;i<n;i++)
        cin>>range[i].l>>range[i].r;

    sort(range,range+n);

    int res=0,end=-1e10+10;
    for(int i=0;i<n;i++)
        if(end<range[i].l){
            res++;
            end=range[i].r;
        }
    cout<<res<<endl;
}
```

### 最大不相交区间数量

- [435. 无重叠区间](https://leetcode.cn/problems/non-overlapping-intervals/)
- 每个区间按照右端点从小到大排序,从前往后依次枚举每个区间，如果当前区间的左端点在上一个区间中，res++，如果没有，更新当前的右端点

```C
int end = intervals[0][1];
for(int i = 1; i < intervals.size(); i++){
    if(intervals[i][0] < end)
        res++;
    else
        end = intervals[i][1];
}
```

---

- 给定 N 个闭区间 [ai,bi]，请你在数轴上选择若干区间，使得选中的区间之间互不相交（包括端点）。
- 输出可选取区间的最大数量。

---

- 思路：
- 每个区间按照右端点从小到大排序
- 从前往后依次枚举每个区间，如果当前区间中已经包含点，直接continue，否则选择当前区间的右端点
- 要注意，这里不相交包括端点不相交，上一个题可以是端点包含

---

```C
struct Range{
    int l,r;
    bool operator< (const Range& W)const{
        return r < W.r;
    }
}range[N];
sort(range, range + n);
int res = 0, end = range[0].r;
for(int i = 1; i < n; i++)
    if(end <= range[i].l){
        res++;
        end = range[i].r;
    }
cout << res << endl;
```

### 区间分组

- 给定 N 个闭区间 [ai,bi]，请你将这些区间分成若干组，使得每组内部的区间两两之间（包括端点）没有交集，并使得组数尽可能小。
- 输出最小组数。

---

- 思路：
- 将所有的区间按照左端点从小到大排序
- 从前往后处理每个区间，对每个区间判断能否放到某个现有的组中（某组的最后一个区间右端点是否在这个区间的左边），如果不可以，就开辟一个新的组
- 采用小根堆来维护当前所有区间组中最后一个区间的右端点

---

```C
\#include <bits/stdc++.h>
using namespace std;
const int N =100010;
int n;

struct Range{
    int l,r;
    bool operator< (const Range &W)const{
        return l < W.l;
    }
}range[N];
int main(){
    cin.tie(0);
    cout.tie(0);
    ios::sync_with_stdio(false);
    cin>>n;
    for(int i=0;i<n;i++)
        cin>>range[i].l>>range[i].r;

    sort(range,range+n);

   priority_queue<int, vector<int>, greater<int>> heap;
    for (int i = 0; i < n; i ++ ){
        if (heap.empty() || heap.top() >= range[i].l){
            heap.push(range[i].r);
        }
        else {
            heap.pop();
            heap.push(range[i].r);
        }
    }

    cout<< heap.size();
}
```

### 区间覆盖

- 给定 N 个闭区间 [ai,bi] 以及一个线段区间 [s,t]，请你选择尽量少的区间，将指定线段区间完全覆盖。
- 输出最少区间数，如果无法完全覆盖则输出 −1 。

---

- 思路：
- 将所有区间按照左端点从小到大进行排序
- 从前往后枚举每个区间，在所有能覆盖start的区间中，选择右端点的最大区间，然后将start更新成右端点

---

```C
\#include <bits/stdc++.h>
using namespace std;

const int N = 100010;

int n;
struct Range{
    int l, r;
    bool operator< (const Range &W)const{
        return l < W.l;
    }
}range[N];

int main(){
    int st, ed;
    cin>>st>>ed>>n;
    for (int i = 0; i < n; i ++ )
        cin>>range[i].l>>range[i].r;

    sort(range, range + n);

    int res = 0;
    bool success = false;
    for (int i = 0; i < n; i ++ )    {
        int j = i, r = -2e9;
        while (j < n && range[j].l <= st){
            //找到所有可以覆盖st位置的区间中，最大的右端点r
            r = max(r, range[j].r);
            j ++ ;
        }
        if (r < st){
            //如果这个端点没有更新，就说明没有可以覆盖st的区间，break
            res = -1;
            break;
        }
        res ++ ;
        if (r >= ed){
            //如果没有经过这步，说明没有经过ed的区间
            success = true;
            break;
        }
        st = r;
        i = j - 1;
    }
    if (!success)
        res = -1;
    cout<<res;
    return 0;
}
```

### 区间重叠

- [452. 用最少数量的箭引爆气球](https://leetcode.cn/problems/minimum-number-of-arrows-to-burst-balloons)
- 如果气球重叠了，重叠气球气球的最小右边界需要一箭
- 如果没有重叠，就res++，重叠之后，更新一下当前i的右边界，是两个气球中最小右边界，之后遍历过程中，判断下一个是否跟这个重叠，如果没有就需要一箭

```C
static bool cmp(const vector<int>& a, const vector<int>& b) {
    return a[0] < b[0];
}
int findMinArrowShots(vector<vector<int>>& points) {
    if (points.size() == 0) return 0;
    sort(points.begin(), points.end(), cmp);

    int result = 1; // points 不为空至少需要一支箭
    for (int i = 1; i < points.size(); i++) {
        if (points[i][0] > points[i - 1][1]) {  // 气球i和气球i-1不挨着，注意这里不是>=
            result++; // 需要一支箭
        }
        else {  // 气球i和气球i-1挨着
            points[i][1] = min(points[i - 1][1], points[i][1]); // 更新重叠气球最小右边界
        }
    }
    return result;
}
```
