---
tags: 
up: 
  - "[[面向对象]]"
down: 
relation:
  - "[[虚函数与虚函数表]]"
---
- 虚函数的调用依赖于Vtable，而指向虚函数表的指针vptr需要在构造函数中进行初始化，所以无法调用定义为虚函数的构造函数。
- 构造函数的调用顺序是从基类到派生类，因为基类部分先于派生类部分构造。如果基类构造函数是虚函数，那么它将无法正常地被调用，因为在调用虚函数之前必须先构造对象