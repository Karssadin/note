---
tags: 
up:
  - "[[网络层：IP]]"
down: 
relation:
---
- **默认路由**：许多网络中的路由器都配置有一个默认路由（有时称为零路由或缺省路由），指向一个"上游"路由器。如果没有特定的匹配条目，流量就会沿着默认路由转发。
- **DHCP获取的路由**：当主机使用DHCP获取IP地址时，通常也会获得一个**默认网关**地址，该地址通常用作下一跳。
- **动态路由协议**：如果路由器支持并配置了动态路由协议，如RIP、OSPF或BGP，那么它可以从邻近的路由器动态学习路由信息。