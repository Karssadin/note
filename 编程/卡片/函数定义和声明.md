---
tags: 
up: 
  - "[[函数]]"
down: 
relation:
---
- 将常用代码封装起来，减少重复代码
- 实现多个模块
- 定义：1、返回值类型2、函数名3、参数列表4、函数体语句5、return？
    - **有参数和返回值、无参数和返回值、二者中有一个**
- 调用：把参数传递进去就行，如果有返回值可以使用变量接收返回值——函数名(参数)
- 值传递：把实参的值传递给形参，如果**不传递指针**相关的话，形参发生变化，**不会影响实参**
    - 地址传递可以通过指针来修改对应的值
- 声明可以有多次，但是定义实现只能一次